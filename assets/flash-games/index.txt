# RetroFlash published games: sha256 TAB size TAB title TAB file TAB poster TAB backdrop
# Published by dcext-push-retroflash.py from repo-setup/assets/flash-games/.
# Games listed in BUNDLED_PACK are not published here: they travel inside the
# extension itself, so listing them again would only duplicate every card.
