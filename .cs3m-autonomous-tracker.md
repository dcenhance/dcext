# CS3M autonomous release tracker

## 2026-07-27 — BLOCKED: published-baseline provenance divergence
- **Audit target:** local CS3M update/version identity and published artifact provenance.
- **Evidence:** clean local branch `cron/dcext-artifact-improve` at `0dfb0eddfbf85fe55afef5596a68002142ff8b43` contains CS3M v631 (`plugins.json`, `moditions/version.json`, artifact manifest) with artifact SHA-256 `87dbce12dbd2286e2fba889947ad816e6aa5469ae2691cca32fa03646d8ae4f7` (1,776,084 bytes). Its deployed DEX additionally exposes stale `CS3MVersion.VERSION_CODE=0x272` / `VERSION_NAME=1.0.626`, while its update checker also initializes `CURRENT_VERSION_CODE=0x272`.
- **Published baseline check:** GitHub `main` resolves to `b42eba8e4aa0de4a77805e634b7fd6f48bf5ad6c`; raw `plugins.json` and `moditions/version.json` declare v648 and raw `CS3MProvider.cs3` is a valid 148-entry ZIP with `classes.dex`, manifest version 648, SHA-256 `8df13bf67d3e5d01c283ff8ae534838f7ea109ca039d682cde4512eb72e42ee1` (1,822,944 bytes), no traversal entries, and successful ZIP CRC test.
- **Decision:** no artifact/source edit or publish. The isolated branch’s v631 artifact is not the current published baseline (v648); applying a repair against it would violate the artifact-first provenance gate. Reconcile this worktree with the authenticated published baseline before a future release worker evaluates the stale in-artifact version constants.
- **Verification:** non-blocking `/tmp/cs3m-autonomous-publish.lock` acquired; worktree was clean before audit and restored clean after removal of audit extraction by-products. No device/runtime QA claimed.

## 2026-07-27 — AUDIT: v648 baseline coherent; no safe narrow repair
- **Audit target:** deployed CS3M v648 artifact, version identity, and ZIP/DEX structural integrity.
- **Evidence:** local `plugins/CS3MProvider.cs3` and GitHub raw `main` are byte-identical: SHA-256 `8df13bf67d3e5d01c283ff8ae534838f7ea109ca039d682cde4512eb72e42ee1`, 1,822,944 bytes. Both have 148 ZIP entries, `classes.dex`, no traversal names, and clean CRC. Manifest version is 648; remote `plugins.json` has `version`/`versionCode`/`versionInt` 648 and fileSize 1,822,944; remote `moditions/version.json` has `versionName`/`latestVersion` `1.0.648` and `versionCode` 648. Baksmali decompilation of the deployed `classes.dex` succeeded; `CS3MVersion.VERSION_CODE=0x288` and `VERSION_NAME=1.0.648` agree.
- **Decision:** no source-derived class family is present in this artifact-only worktree, and the previously observed stale-version defect is resolved in the published baseline. No evidence-backed, safely verifiable functional change was available; no artifact or release metadata was modified and nothing was published.
- **Verification:** lock held; temporary audit output was removed and the worktree passed `git diff --check`. No device/runtime QA claimed.

## 2026-07-27 — AUDIT: v648 Discord lifecycle/default path verified; no safe source-derived repair
- **Audit target:** deployed v648 Discord artwork preference and delayed-presence lifecycle, compared with v646/v647 published artifacts.
- **Evidence:** local and GitHub-raw v648 artifacts remain byte-identical: SHA-256 `8df13bf67d3e5d01c283ff8ae534838f7ea109ca039d682cde4512eb72e42ee1`, 1,822,944 bytes; ZIP has 148 unique non-traversal entries, `classes.dex`, valid CRC, and manifest version 648. Baksmali successfully disassembled 2,272 classes. v647→v648 changed exactly `CS3MVersion`, `DiscordModitions`, `CS3MSettingsFragment`, and `DiscordSettingsFragment`; version constants are `0x288`/`1.0.648`. Both settings UIs write canonical `discord_show_poster` with default `false`; runtime migration uses `contains()` before writing false and the runtime reader defaults false, preserving an existing explicit choice. The delayed start clears its retained runnable then revalidates enabled/watching/logged-in state plus the monitor weak-reference; stop/cleanup cancel monitor, burst, artwork, grace, and pending callbacks.
- **Decision:** no changed source class family exists in this artifact-only worktree and the audited path already satisfies default-off, canonical-key, revalidation, and cleanup requirements. No safe evidence-backed repair, metadata change, or publish.
- **Verification:** non-blocking lock held; v646/v647/v648 artifacts decompiled outside the worktree; `git diff --check` passed before tracker update. No device/runtime QA claimed.

## 2026-07-27 — AUDIT: v648 settings-injection listener lifecycle is complete; no safe repair
- **Audit target:** deployed v648 CS3M settings-entry injection, its persistent/global-layout listener, and one-shot retry listener cleanup.
- **Evidence:** the GitHub-raw artifact remains byte-identical to local baseline: SHA-256 `8df13bf67d3e5d01c283ff8ae534838f7ea109ca039d682cde4512eb72e42ee1`, 1,822,944 bytes. ZIP validation found 148 unique non-traversal entries, `classes.dex`, and clean CRC; manifest and all repository/update version fields are 648 / `1.0.648`. Baseline backup was saved outside the worktree. `dexdump` completed and APKTool baksmali decompiled all 2,272 classes outside the worktree. `CS3Moditions.addSettingsLayoutListener(Activity)` retains the exact `(root, listener)` pair; `unregisterActivityCallbacks(Activity)` removes that same listener and all pending injection/bootstrap callbacks. The one-shot `injectSettingsOnLayout` registration checks observer liveness, removes the exact prior/current listener, and releases its owner after successful injection.
- **Decision:** the audited lifecycle already satisfies retained-instance, cancellation, and cleanup requirements. No source-derived class family exists in this artifact-only worktree and no safe evidence-backed functional change, version bump, or publish was made.
- **Verification:** local artifact metadata and ZIP integrity rechecked after audit; `git diff --check` passed before tracker update. No device/runtime QA claimed.

## 2026-07-27 — AUDIT: v648 update-download integrity guardrails verified; no safe repair
- **Audit target:** deployed v648 `UpdateChecker` download, archive-validation, and installation path.
- **Evidence:** local artifact and GitHub-raw `main` are byte-identical: SHA-256 `8df13bf67d3e5d01c283ff8ae534838f7ea109ca039d682cde4512eb72e42ee1`, 1,822,944 bytes. Both ZIPs have 148 entries, valid CRC, `classes.dex`, and manifest version 648; remote `plugins.json` version/versionCode/versionInt and `moditions/version.json` versionCode are all 648. Direct DEX inspection (Androguard) shows `UpdateChecker.downloadFile` first calls `isTrustedUpdateUrl`, disables HTTP redirects, requires HTTP 200, applies 30s connect/60s read limits, enforces advertised content length and a bounded maximum. `isValidPluginArchive` rejects non-files, zero/over-25MiB inputs, >4,096 entries, absolute/traversal/backslash names, and archives missing `classes.dex`; installation uses `.part`/`.bak` and reports only after archive verification.
- **Decision:** this deployed path already has URL, transfer, archive, and atomic-replacement checks. No matching editable source/class family or verified DEX assembly tool is present in this artifact-only worktree, so no safe narrow functional repair, version bump, or publication was made.
- **Verification:** non-blocking lock held; temporary DEX/remote audit files stayed outside the worktree, `git diff --check` passed before tracker update. No device/runtime QA claimed.

## 2026-07-27 — AUDIT: v648 Watch-Party-Player-Injektion vollständig; keine sichere Reparatur
- **Audit target:** deployed v648 Syncplay Watch-Party preference, player-button retry, and teardown lifecycle.
- **Evidence:** local `plugins/CS3MProvider.cs3` and GitHub-raw `main` are byte-identical: SHA-256 `8df13bf67d3e5d01c283ff8ae534838f7ea109ca039d682cde4512eb72e42ee1`, 1,822,944 bytes. The ZIP has 148 unique non-traversal entries, `classes.dex`, manifest version 648, and clean CRC. `dexdump -d` completed for all 2,272 classes. `PlayerSettingsFragment` creates the `watch_party_enabled` `SwitchPreferenceCompat`, writes default `false`, reads that same canonical key with default `false`, and its listener persists the key, calls `WatchPartyManager.cleanup()` on disable, then refreshes injections. `WatchPartyManager.apply()` independently reads both `cs3m_enabled` and `watch_party_enabled` with default `false`; disabled state removes the button and disconnects. The bounded 250/750/1500/2500-ms retries retain each exact `Runnable` in `pendingButtonRetries`; completion removes itself, requires attached root plus non-finishing/non-destroyed activity, then re-enters `apply()`. Per-root success removes callbacks; `cleanup()` removes every pending callback, clears the map, and releases weak references.
- **Decision:** no source Kotlin/Smali family exists in this artifact-only worktree; the v646 historical change also contains only binary artifact/metadata paths. An artifact-first repair cannot be provenance-proven or round-trip assembled here, and the deployed path already meets default-off, revalidation, and cleanup requirements. No functional change, version bump, or publish.
- **Verification:** remote bytes/hash and repository metadata remain v648; worktree was clean before this tracker entry. No device/runtime QA claimed.

## 2026-07-27 — BLOCKED: Audio-Output-Indikator Callback-Provenienz/Reinigung
- **Audit target:** v648 `AudioOutputModitions` Broadcast-/Polling-Callback-Lifecycle im veröffentlichten Artefakt.
- **Evidence:** lokales und GitHub-raw-Artefakt sind byte-identisch (SHA-256 `8df13bf67d3e5d01c283ff8ae534838f7ea109ca039d682cde4512eb72e42ee1`, 1,822,944 bytes); ZIP: 148 eindeutige, traversalfreie Einträge, `classes.dex`, CRC OK, Manifest/remote Metadaten v648/`1.0.648`. `dexdump -d` zeigt: `teardownPrevious()` entfernt den gespeicherten Poll und deregistriert den gespeicherten Receiver; der Poll prüft `rootView.isAttachedToWindow()` und räumt dann auf. `AudioOutputModitions$apply$audioReceiver$1.onReceive` erzeugt dagegen pro Broadcast eine neue Lambda und postet sie, ohne sie zu speichern oder vor dem UI-Update Attachment/Generation zu revalidieren; nach `cleanup()` kann ein bereits gepostetes Update noch auf die alte `ImageView` wirken.
- **Blocker/Decision:** Keine aktuelle Kotlin-/Smali-Klassenfamilie im Artefakt-Worktree; einziges historisches Kotlin (`3a976d6`, 2026-07-11) ist keinem nachweisbar identischen Build-Artefakt zugeordnet. `baksmali`, `smali` und `apktool` fehlen. Daher keine provenance-sichere Klassenfamilien-Änderung, kein Round-trip-Assembly, kein Versionsbump und kein Publish.
- **Verification:** Baseline vor Audit sauber bei `c92d03c`; Fetch ergab `origin/main` als Merge-Base (behind 0), Lock gehalten; keine Geräte-/Runtime-QA behauptet.

## 2026-07-27 — RELEASE: v649 Audio-Output-Callback-Lebenszyklus
- **Audit/Evidence:** v648 hatte `show_audio_output` als Default `false` und einen kanonischen UI-Writer/Runtime-Reader, aber `CS3Moditions` rief bei einem Disable-Refresh kein `AudioOutputModitions.cleanup()` auf. Der BroadcastReceiver postete außerdem pro Empfang eine nicht gespeicherte Lambda; ein bereits eingereihter UI-Update konnte nach Teardown noch die alte `ImageView` erreichen.
- **Änderung:** Artefakt-first, DEX-Round-trip der vollständigen 2.272-Klassen-Datei; geändert wurden `CS3Moditions`, `AudioOutputModitions`, dessen Receiver und Synthetic-Runnable sowie `CS3MVersion`. Disable ruft jetzt `cleanup()` auf; genau ein gespeichertes Broadcast-Runnable wird vor Ersatz/Teardown entfernt, beim Lauf als aktuell geprüft und nur bei angehängter Ziel-View ausgeführt. Präferenz bleibt Default-off.
- **Release:** v649 / `1.0.649`, Commit `830c0a44c2e1e06a63eb6adac29811cd84649151`, SHA-256 `9c4312f10e7212ede65f6903c357265cfb398cb6889b0a537751bd7b3247ef59`, 1.817.063 Bytes.
- **Verifikation:** Baseline gesichert (`03057b5`, v648 SHA `8df13bf67d3e5d01c283ff8ae534838f7ea109ca039d682cde4512eb72e42ee1`); vor/nach Baksmali+Smali und finaler DEX-Dump OK, nur `classes.dex`/`manifest.json` und exakt fünf Smali-Klassen geändert. ZIP: 148 eindeutige traversalfreie Einträge, `classes.dex`, CRC OK; Metadata/Manifest/DEX-Version konsistent 649. Authentifizierter Push nach `main` OK; commit-fixiertes GitHub-Raw und Remote-Git-Metadaten byte-identisch, Hash/Größe/Version bestätigt. Keine Geräte-/Runtime-QA behauptet.

## 2026-07-27 — BLOCKED: v650 Now-Playing-Overlay-Release nicht authentifizierbar
- **Audit/Änderung:** Das veröffentlichte v649-DEX postete den 4-s-Fade des `NowPlayingOverlayModitions` direkt auf die Overlay-View, ohne die Runnable-Instanz für Disable/Teardown zu halten. v650 speichert Overlay plus exaktes Runnable, entfernt es in `stopMonitoring()` (damit auch Disable/`clearTracking()`), und das Runnable prüft bei Ausführung Identität sowie Attachment von Overlay und Root.
- **Artefakt-first-Verifikation:** Baseline `94efe2c` / v649 SHA `9c4312f10e7212ede65f6903c357265cfb398cb6889b0a537751bd7b3247ef59` gesichert. Baksmali→Smali→Baksmali und `dexdump` OK; exakt geändert: `CS3MVersion`, `NowPlayingOverlayModitions`, `NowPlayingOverlayModitions$$ExternalSyntheticLambda1`. ZIP v650: 148 eindeutige traversalfreie Einträge, `classes.dex`, CRC OK; 146 Einträge bytegleich erhalten, nur `classes.dex` und `manifest.json` geändert. Manifest/DEX/`plugins.json`/`moditions/version.json` konsistent v650 / `1.0.650`. Lokales Artefakt: SHA-256 `243d7eb1a3153c7a24211cc75d31a65deb7abed810106d6f39e324416e455174`, 1.825.714 Bytes; lokaler Commit `3372dac14e3106f8f027e6733b0570301faeb746`.
- **Blocker:** `git push origin HEAD:main` schlug fehl: `fatal: could not read Username for 'https://github.com': No such device or address`. Keine Veröffentlichung erfolgt; `origin/main` blieb vor dem Versuch auf `94efe2c`. Keine Geräte-/Runtime-QA behauptet.

## 2026-07-27 — BLOCKED: v650-Release-Pfad weiterhin nicht authentifiziert
- **Audit:** Der veröffentlichte GitHub-Raw-Baselinezustand ist weiterhin v649: SHA-256 `9c4312f10e7212ede65f6903c357265cfb398cb6889b0a537751bd7b3247ef59`, 1.817.063 Bytes; Remote-Metadaten und `moditions/version.json` sind konsistent 649 / `1.0.649`. Das lokal vollständig verifizierte v650-Artefakt bleibt SHA-256 `243d7eb1a3153c7a24211cc75d31a65deb7abed810106d6f39e324416e455174`, 1.825.714 Bytes.
- **Blocker/Verifikation:** `gh` ist nicht installiert; ein schreibfreier `git push --dry-run origin HEAD:main` endet erneut mit `fatal: could not read Username for 'https://github.com': No such device or address` (Exit 128). Daher kein Publish-Versuch und keine weitere Artefaktänderung. Lock gehalten; auditbedingt extrahierte, ungetrackte ZIP-Dateien entfernt und Worktree anschließend sauber geprüft. Keine Geräte-/Runtime-QA behauptet.

## 2026-07-27 — RELEASE: v650 Now-Playing-Overlay-Callback-Lebenszyklus
- **Audit/Änderung:** Veröffentlichtes v649 hielt den 4-s-Fade des Now-Playing-Overlays nicht für Teardown vor. v650 speichert die exakte Fade-Runnable, entfernt sie in `stopMonitoring()`/`clearTracking()`, und prüft bei Ausführung aktuelle Runnable-Identität sowie Attachment von Overlay und Root. Geändert im DEX: `CS3MVersion`, `NowPlayingOverlayModitions`, `NowPlayingOverlayModitions$$ExternalSyntheticLambda1`; keine neue Präferenz, bestehende Funktionalität bleibt unverändert default-off.
- **Artefakt-Gate:** Remote-v649-Baseline `94efe2c`/SHA `9c4312f10e7212ede65f6903c357265cfb398cb6889b0a537751bd7b3247ef59` gesichert. Baksmali→Smali→Baksmali: 2.272 Klassen und semantisch identisch; DEX-Dump erfolgreich. Der zunächst lokale v650-ZIP-Entwurf änderte zusätzlich Ressourcen und wurde nicht publiziert. Finales Artefakt übernimmt aus der Baseline ausschließlich `classes.dex` und `manifest.json`; 146/148 Eintragsinhalte sind erhalten, ZIP/CRC/traversal-Prüfung OK.
- **Release/Remote-Verifikation:** v650 / `1.0.650`, Commit `2ab0923a1aa5c4012c69a56da1bdb952d34b1310`, SHA-256 `e7f3d44576748acf73560a4807b862051715e623c10c4afbbda1cc2229894ccc`, 1.823.340 Bytes. `plugins.json` (`version`/`versionCode`/`versionInt`, Größe), `moditions/version.json`, Manifest und DEX (`0x28a`/`1.0.650`) konsistent. Authentifizierter Push nach `main` OK; GitHub API und commit-fixiertes sowie `main`-Raw sind byte-identisch, 148 eindeutige Einträge mit `classes.dex`, CRC OK. Keine Geräte-/Runtime-QA behauptet.
