{
  "versionName": "1.0.786",
  "latestVersion": "1.0.786",
  "versionCode": 786,
  "changelog": [
    "Remove the duplicate status-bar padding above the CS Manager top bar",
    "Render every settings icon as deterministic white 24dp artwork from the first frame",
    "Match CloudStream's native 56dp preference geometry and remove the duplicate top inset",
    "Refresh dependent switches and sliders immediately when their controlling preference changes",
    "Use pure white 24dp icons without tinted icon tiles across every CSM settings page",
    "Remove the duplicated status-bar inset and modernize row spacing while preserving gesture-navigation clearance",
    "Keep the injected CS Manager entry neutral and place it last among CloudStream settings categories",
    "Redesign CSM settings with compact modern rows, consistent white icons and navigation chevrons",
    "Apply the status-bar inset exactly once and keep the final row clear of gesture navigation",
    "Keep the CS Manager toolbar and injected CloudStream entry in the host's neutral text color",
    "Use pure white icons for every CSM settings icon",
    "Match CSM settings alignment, spacing, typography, icon tint and switch colors to CloudStream 4.8",
    "Show the current CS Manager version in CSM and in CloudStream's injected settings entry",
    "Apply special list animations to CloudStream Compose surfaces and reliably cancel them when reset",
    "Keep explicit Compose calls that avoid incompatible default-argument ABI bridges",
    "Fix the CSM settings crash on CloudStream builds whose Compose LazyListScope ABI does not expose item$default",
    "Call LazyColumn item and indexed-item APIs with all arguments explicitly for compatibility across CloudStream 4.8 builds",
    "Remove the remaining Compose default-argument ABI bridges used by the CSM settings renderer",
    "Fix the CSM settings crash on CloudStream builds whose Compose RowScope ABI does not expose the newer default-argument bridge",
    "Call Compose row weight with all parameters explicitly for compatibility across CloudStream 4.8 builds",
    "Render every CSM settings page with CloudStream 4.8 Compose styling while preserving existing preferences, dialogs and dependencies",
    "Keep CSM search, native NavController Back behavior, Discord profile cards and advancement medal actions working in Compose",
    "Use CloudStream theme colors and apply CSM typography to both CloudStream and CSM Compose views",
    "Apply CSM app, title and body fonts to CloudStream's new Compose text views",
    "Apply font scale, letter spacing and line height to live Compose text without replacing the host composition",
    "Index every CSM preference title in CSM search while preserving curated actions and availability filters",
    "Use CloudStream's native NavController for every CSM toolbar Back action",
    "Remove the broad toolbar touch strip so only the real navigation icon handles Back",
    "Fix category crashes by registering every CSM settings page as a native CloudStream FragmentNavigator destination",
    "Navigate Appearance and all other CSM categories through CloudStream's NavController instead of direct FragmentManager transactions",
    "Integrate CSM directly into CloudStream's SettingsFragmentScreen model instead of drawing a View overlay",
    "Open CSM through CloudStream's real NavController so the bottom tab navigation remains visible",
    "Install the native settings item before the settings composition is created, eliminating late injection",
    "Render the CSM row continuously as CloudStream's settings list scrolls instead of waiting for the row and footer to fit completely",
    "Clip partial CSM and build-footer frames to the settings viewport so the bottom navigation remains untouched",
    "Measure the injected row before positioning and reserve its real height to remove delayed or missing injection",
    "Keep Extensions, the original build footer and bottom navigation visible without late overlay clipping",
    "Remove and suppress the CloudStream landing-page CSM row while CSM's own settings screen is open",
    "Keep CloudStream's existing settings column scrollable while reserving one native CSM row",
    "Keep the bottom tab navigation visible and prevent the CSM row or build footer from covering it",
    "Inject the CSM row with frame-sized retries instead of a visible quarter-second delay",
    "Use CloudStream's native extension icon metrics and preserve the complete build line on narrow phones",
    "Restore the CSM settings row on CloudStream Compose 1.12 by traversing its current root semantics tree",
    "Read the build-stamp text through the current SemanticsConfiguration.get API while retaining older-host compatibility",
    "Show CSM directly below Extensions when Compose merges the native build-stamp semantics",
    "Keep the anchor bounded to the 50dp native build row so no settings category can be covered",
    "Fixed the CSM row covering Extensions and duplicating CloudStream's build stamp",
    "Anchor the CSM row to the exact build-stamp text leaf and preserve the complete Extensions row",
    "Match CloudStream's primary settings background without a tinted CSM band",
    "Added CSM directly below Extensions and above CloudStream's build stamp using the native settings-row metrics",
    "Removed the custom main-settings replacement and all floating fallback UI",
    "Aligned CSM with the latest committed CloudStream 4.8 settings, player, result and plugin contracts",
    "Preserved CloudStream's native Compose settings screen and fail closed on unknown host destinations",
    "Updated episode, buffering, download, plugin, diagnostics and player targets with bounded current-first lookups",
    "Aligned the build lane and Media3 API with CloudStream while producing a deterministic single-DEX plugin",
    "Added safe previous-identity settings and installation migration without emitting legacy branding",
    "Preserve queued generation-aware refreshes across Activity destruction and replay them on a valid replacement host",
    "Stop the Audio Output receiver and polling loop when player injections are cleared, and use a monotonic clock for scheduler deduplication",
    "Own pending settings layout listeners per Activity/root and remove them on success, root replacement, refresh-safe transitions, and Activity destruction",
    "Send the verified HTTPS Discord attachment URL directly as outbound large_image, matching Discord's current Gateway Activity Asset Image contract",
    "Added a selectable physical-device Artwork delivery test with Auto, Discord CDN, Discord Media CDN, mp:attachments and original provider URL methods",
    "Added persistent credential-free artwork diagnostics containing the selected method and resolution stage",
    "Direct/Custom Discord RPC and Kizzy/Broadcast RPC are mutually exclusive again to prevent competing presence writers",
    "A configured Direct login owns presence updates; Kizzy is used only when Direct login is absent",
    "Kizzy keeps its explicit package-targeted broadcast for modern Android compatibility",
    "Fixed RPC artwork uploads being skipped unless a direct Discord account token was present; hardcoded webhook uploads now run for direct and broadcast/Kizzy RPC",
    "Fixed asynchronous upload completion refreshing only direct-login RPC; all enabled watching-presence modes now receive the mapped attachment",
    "Added PNG, JPEG, GIF and WebP signature detection for image CDNs that return generic content types",
    "Hardcoded RPC webhook is now AES-256-GCM encrypted with fragmented key, nonce and ciphertext material inside the plugin",
    "The plaintext webhook URL and webhook ID are absent from the shipped DEX; runtime decryption is integrity-authenticated",
    "Discord RPC artwork now downloads and uploads directly through the intentionally hardcoded Discord webhook",
    "RPC uses stable mp:attachments assets returned by Discord instead of provider image URLs",
    "Direct webhook uploads enforce HTTPS sources, image MIME allowlisting and an 8 MiB limit",
    "Dedicated activity-level Back callback now owns the CSM settings overlay lifecycle",
    "Side panels fall back to CloudStream native controls when player data is unavailable",
    "Failed reflected episode/source row actions now recover through native controls instead of closing silently",
    "Fixed progress bar dropping below other controls during player hide animation",
    "Fixed system Back and gesture navigation for the CSM settings overlay",
    "CSM settings now block background touches and navigation",
    "Fixed Episodes and Sources Side Panel buttons becoming unresponsive",
    "Side panels preserve CloudStream native controls and fall back safely",
    "Visible premium Contributors card for JinDevil with Discord API profile data, avatar and decoration",
    "Fixed CSM installation URL and added builds-branch compatibility",
    "Persistent CSM toggle state across settings reloads and fragment rebuilds",
    "Legacy AndroidX/default preferences migrated into CSMSettings",
    "Discord RPC and all modition switches now rehydrate from one store",
    "Kool VOD now uses current www.kool.to mediahub POST contract (v16)",
    "Kool Live TV now uses current catalog and dynamic HLS resolve contract (v12)",
    "Silent injection by default",
    "Progress overlay is opt-in",
    "View inspector no longer installs a forced global input hook"
  ],
  "minCloudstreamVersion": 1,
  "downloadUrl": "https://raw.githubusercontent.com/dcenhance/dcext/main/plugins/CSMProvider.cs3",
  "releaseDate": "2026-09-16",
  "isRequired": false,
  "message": "Native CloudStream settings geometry, deterministic white icons and live preference dependencies",
  "sha256": "2b613ef8d2044d7e7bc6a1588934be29e7f298532d8a3125ce058b876122ca09"
}
